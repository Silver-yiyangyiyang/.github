# 老人隐私安全与数据安全收口执行计划

状态基线：2026-08-29

主任务：[Silver-yiyangyiyang/.github #7](https://github.com/Silver-yiyangyiyang/.github/issues/7)

范围：规划、证据治理与答辩口径；不在本任务内实现新的生产安全服务。

## 一、目标与完成口径

本计划把已经分散在各仓库的隐私机制，收口为评审可核验、团队可执行、后续可撤回的证据链。它不把代码存在等同于真实老人隐私已经得到生产保护，也不把模板存在等同于已经获得真人授权。

#7 只有同时满足以下条件，才可以从 WIP 进入 Qualified：

1. 隐私机制、代码锚点、测试锚点、证据等级和未收集项保持同一版本；
2. 书面授权模板、撤回办法、受控证据登记方法可以直接执行；
3. 答辩隐私页和提交材料只使用经核验的 L1/L2 句式；
4. 新增生产机制被拆到独立工程 Issue，不在文档中写成已经实现；
5. 真人原件、真机截图、服务器日志和其他敏感材料只留在受控证据库，Git 只保留去敏登记和哈希；
6. 完成一次最终提交包去敏复核，并留下可追溯报告。

Qualified 仍不等于 Ranked。正式接受、所需 PR 合并且答辩材料复核完成后，才可进入 Ranked 并关闭 #7。

## 二、当前已有资产

| 资产 | 当前状态 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| [`PRIVACY_SECURITY_EVIDENCE.md`](https://github.com/Silver-yiyangyiyang/jbgs-proposal/blob/main/docs/PRIVACY_SECURITY_EVIDENCE.md) | 已形成，核验快照为 2026-08-24 | 七仓隐私机制具有代码/测试锚点，L1/L2/L3 口径已定义 | 锚点永远有效；真实老人或生产长期运行已经验证 |
| [`ELDER_WRITTEN_CONSENT_TEMPLATE.md`](https://github.com/Silver-yiyangyiyang/jbgs-proposal/blob/main/docs/ELDER_WRITTEN_CONSENT_TEMPLATE.md) | 模板已就绪 | 真人出镜、录音、门前媒体和真机记录有逐项勾选模板 | 已取得实名签署原件 |
| [`DEFENSE_PPT_REFERENCE.md`](https://github.com/Silver-yiyangyiyang/jbgs-proposal/blob/main/docs/DEFENSE_PPT_REFERENCE.md) §8.1 | 口头句式已形成 | 答辩可以按证据等级陈述机制与边界 | 句式已经进入最终 PPT 或已经过现场复核 |
| [`SUBMISSION_CHECKLIST.md`](https://github.com/Silver-yiyangyiyang/jbgs-proposal/blob/main/docs/SUBMISSION_CHECKLIST.md) | 有提交红线与人工材料缺口 | 可约束提交包不包含凭据、数据、权重和原始媒体 | 最终锁版包已经人工逐文件复核 |

## 三、证据等级与状态字段

### 3.1 证据等级

- **L1 工程验证**：代码、自动化测试、静态检查或可复现的合成链路。
- **L2 指定设备/受控环境**：指定手机、门前设备或服务器窗口的去敏记录。
- **L3 生产/真人**：真实授权主体、真实生产身份、长期访问审计和可撤回证据。

L1/L2 不得改写为 L3。没有当前证据时，应写“未收集”或“blocked”，不能用“计划中”代替通过。

### 3.2 去敏证据登记最小字段

Git 中只允许登记以下字段：

| 字段 | 说明 |
|---|---|
| `evidence_id` | 项目内随机编号，不含姓名、手机号、设备序列号或案件号 |
| `evidence_type` | `consent` / `device_test` / `access_audit` / `package_review` |
| `scope` | 已授权或已验证的最小范围 |
| `evidence_level` | `L1` / `L2` / `L3` |
| `collected_at` | 时间戳，不记录精确住址 |
| `artifact_sha256` | 受控证据文件的 SHA-256；不得提交原文件 |
| `storage_owner_role` | 负责保管的角色，不写个人联系方式 |
| `withdrawal_status` | `active` / `withdrawn` / `not_applicable` |
| `review_status` | `pending` / `accepted` / `rejected` |

禁止登记访问令牌、AppSecret、设备验证码、真实设备标识、原始门前媒体、身份证号、手机号、住址和签名扫描件。

## 四、工作包与验收标准

### P7-A：刷新隐私证据锚点

类型：离线可做，规划/证据核验。

输入：七仓最新 `origin/main`、现有证据清单、对应测试文件。

输出：更新后的 `jbgs-proposal/docs/PRIVACY_SECURITY_EVIDENCE.md`。

执行要求：

1. 记录七仓精确提交，不用工作区当前分支代替 `origin/main`；
2. 逐条确认文件、符号和测试名仍存在；行号仅作辅助，不作为唯一锚点；
3. 新出现的机制必须同时给出代码、测试、边界和证据等级；
4. 已失效锚点必须删除或标成历史，不得保留“看起来完整”的错误引用；
5. 重新统计 L1/L2/L3 与未收集项。

验收：锚点全部可定位；自动测试数字有日期和 commit；没有把开放 PR 写入 `main` 已完成项；没有把研究 ASR、合成数据或指定设备记录越级写成生产效果。

### P7-B：书面授权与撤回登记

类型：需要在场人员；不提交实名材料。

输入：书面授权模板、演示/真机测试的具体范围、受控存储位置。

输出：线下签署原件或“本轮不使用真人”的明确决定，以及一条去敏证据登记。

执行要求：

1. 门前抓拍、门外音频、演示出镜、真机操作分别勾选，不得捆绑授权；
2. 签署前说明用途、保存期、访问角色、撤回方法和赛事平台提交后的限制；
3. 原件放受控存储，不上传 Git、Issue、PR、群聊或普通网盘；
4. 撤回后停止新增采集，更新 `withdrawal_status`，并按既定规则处理已提交赛事平台的材料；
5. 如果不使用真人，演示材料必须明确标为合成/模拟，不得制造真人授权记录。

验收：授权范围与实际采集逐项一致；Git 中只有随机 `evidence_id` 和哈希；能够演示撤回后的停止采集与状态更新。

### P7-C：真机授权、拒绝与撤回三路径

类型：需要微信开发者工具/真机；当前未收集。跨仓真机待验总表见 [`REAL_DEVICE_PENDING.md`](REAL_DEVICE_PENDING.md) §6。

输入：正式导入包、测试账号、合法域名、最小权限手机。

输出：Android/iOS 或明确限定的平台结果、时间戳、失败项和去敏证据哈希。

最少覆盖：首次授权、拒绝授权、系统设置撤回、项目内逐项撤回、会话过期、离线失败关闭。测试时不得使用真实支付、真实诈骗转账或未授权联系人数据。

验收：每条路径有预期与实测；拒绝/撤回后不继续采集；客户端不保留 AppSecret、长期令牌、验证码或原始媒体。浏览器预览不能替代本项。

### P7-D：最终提交包去敏复核

类型：离线自动检查 + 人工逐文件复核。

输入：最终固定 commit、最终 PDF/PPT、源码包和报告。

输出：`package-review` 类型的去敏登记和检查报告。

检查范围：

- 解包后检查 `.env`、密钥、数据库、模型权重、缓存、日志和原始媒体；
- 扫描 AppSecret、accessToken、设备验证码、服务器凭据和真实设备标识模式；
- 检查 PDF/PPT 截图是否露出姓名、手机号、住址、二维码、后台账号或平台密钥；
- 抽查授权、合成、历史、设备、真人和生产证据是否分栏；
- 对超 20 MB 外链记录访问期限和失效回收办法，但不在公开仓保存受限链接凭据。

验收：自动检查无未处置命中；人工逐文件复核完成；报告绑定最终提交 commit 和包 SHA-256。脚本“无命中”不能替代人工抽查。

### P7-E：生产访问审计工程子任务（[Silver #77](https://github.com/Silver-yiyangyiyang/silver-fraud-guard/issues/77)）

类型：独立工程 Issue 已建立，状态为 Pending / Unclaimed；#7 只定义验收，不实现。

建议责任仓：`silver-fraud-guard`，必要时由 Home/Clients 提供会话来源字段。

最小事件字段：

- 服务端派生主体与角色的不可逆引用；
- 动作、资源类型、结果、时间、请求/追踪 ID；
- 授权依据、撤回状态和数据范围；
- 不记录请求正文、原始媒体、令牌、验证码和完整设备标识。

最低验收：

1. 关键读取、下载、授权、撤回、删除和管理动作可审计；
2. 普通业务角色不能修改或删除审计记录；
3. 有保留期、导出、访问复核、完整性校验和故障失败策略；
4. 自动测试覆盖越权、重放、敏感字段去除、撤回和保留期；
5. 先以 L1 工程候选交付，取得真实生产记录后才能升级为 L3。

### P7-F：答辩和提交材料映射（冻结阶段人工材料总任务 [#12](https://github.com/Silver-yiyangyiyang/.github/issues/12)）

类型：规划 + 人工定稿。#12 当前保持 Idle，只有项目进入功能/材料冻结后才激活；统一登记签名、盖章、人工复核、受控归档、平台上传和回执复下载。

输入：最终证据清单、答辩参考、最终 PPT 和提交清单。

输出：一页隐私安全答辩页、逐句复核记录，以及 #12 定义的去敏《人工处理材料总表》。实名签署原件、证件、联系人、账号和原始音视频只进入受控存储，GitHub 仅记录版本、状态和 SHA-256。

必须出现：未核验数据不训练、当前事件决定即时风险、逐项授权与撤回、社区不接收原始门前媒体、前端不保存服务端凭据、证据等级与未收集项。

禁止出现：“已经真实保护全部老人数据”“已经完成生产身份审计”“已获得真人授权”“社区公共区域持续监控”等无对应证据的表述。

验收：每一句可追到证据清单条目；PPT、计划书、演示视频和口头回答不互相矛盾。

## 五、依赖与推荐顺序

```text
P7-A 锚点刷新 ───────────────┐
                             ├─> P7-F 答辩/材料映射
P7-B 书面授权 ─> P7-C 真机 ──┤
                             └─> P7-D 最终包复核 ─> #7 Qualified

P7-E 生产访问审计：独立工程 Issue，可并行；未完成时保持 L3 未收集
```

推荐顺序：先完成 P7-A；并行准备 P7-B 和 P7-E 子任务；有真人/真机窗口后执行 P7-C；材料锁版后执行 P7-D；最后做 P7-F 逐句复核。

## 六、#7 验收矩阵

| #7 验收项 | 当前状态 | 进入完成所需证据 |
|---|---|---|
| `PRIVACY_SECURITY_EVIDENCE.md` | 已有，需按最终 commit 刷新 | P7-A 报告和精确提交 |
| 答辩 PPT 每句有证据等级 | 参考句式已有，最终 PPT 未复核 | P7-F 逐句映射 |
| 书面授权模板就绪 | 已完成 | 模板路径；实名原件仍单列未收集 |
| 不混写授权/合成/真人证据 | 文档门禁已有 | P7-D/P7-F 最终复核 |
| 数据泄露响应与生产访问审计口径 | Silver #77 已建立，工程实现未开始 | P7-E 独立 Issue 的实现与验收证据 |

## 七、立即下一步

1. 对七仓最新 `origin/main` 执行 P7-A 锚点刷新；
2. Silver #77 已记录 P7-E 最低验收；保持 Pending / Unclaimed，待核对当前 Silver PR 所有权后再独立认领；
3. 在真人/真机窗口前，由负责人选择“使用真人并签署”或“完全不使用真人”，不得保持模糊状态；
4. 最终材料锁版时激活 #12，并执行 P7-D 和 P7-F；签名/盖章、复核、上传与回执闭环满足后再请求 #7 进入 Qualified。

## 八、失败关闭原则

- 缺授权：不采集、不展示、不训练；
- 缺身份或授权依据：拒绝访问门前媒体和敏感资源；
- 缺证据：状态保持 `pending` / `blocked`，不推断通过；
- 证据撤回或哈希不匹配：停止使用并重新评审；
- 生产审计未部署：只能声明 L1/L2，不能声明 L3；
- 最终包发现敏感项：停止发布，修复后重新生成全包与哈希。
